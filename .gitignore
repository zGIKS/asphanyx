# .gitignore mínimo: sólo entradas usadas actualmente en el repo

# Rust build artifacts
/target/

# Environment files (están presentes en la raíz)
.env
.env.*

# Logs (si aparecen durante ejecución local)
*.log

# Swap/backup temporales de editores (útil aunque no esté en repo)
*~
*.swp

# MacOS thumbnails (no está en el repo pero es inofensivo)
.DS_Store
